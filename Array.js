//===============Implemeting Array===============
class MArray{
    //Cosntructor
    constructor(){
        this.length = 0;
        this.data = {};
    }
    //get()
    //access the data
    get(index){
        return this.data[index];
    }; 
    
    //push()
    push(item){};

    //pop()
    pop(){};

    //delete()
    delete(){};

    // shift all numbers
    shifItems(index){};
}

const newArray = new MArray();
console.log(newArray);
